spring:
  application:
    name: gateway
  profiles:
    default: default
server:
  port: 4040
eureka:
  client:
    fetch-registry: false
    service-url:
      default-zone: http://localhost:8761/eureka/ 
